pipeline {
    agent any
    stages{
        stage('Build Docker Images') {
            steps {
                sh 'docker buiild -f Dockerfile-aws-cli -t my-aws-cli .'
                sh 'docker build -f Dockerfile-playwirght -t my-playwright2 .'
            }
        }
    }
}